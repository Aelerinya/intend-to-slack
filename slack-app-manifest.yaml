display_information:
  name: Lightcone work bot
  description: Daily/weekly work summaries from Intend.do
  background_color: "#4A154B"

oauth_config:
  scopes:
    user:
      - chat:write

settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
